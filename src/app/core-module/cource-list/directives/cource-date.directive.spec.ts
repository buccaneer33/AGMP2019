import { CourceDateDirective } from './cource-date.directive';

describe('CourceDateDirective', () => {
  it('should create an instance', () => {
    const directive = new CourceDateDirective();
    expect(directive).toBeTruthy();
  });
});
